import {
  data as UserData,
  columns as UserColumns,
} from '@/utils/User'
import {
  data as PoemData,
  columns as PoemColumns,
} from '@/utils/Poem'
import {
  data as CommentData,
  columns as CommentColumns,
} from '@/utils/Comment'

export {
  UserData,
  UserColumns,
  PoemData,
  PoemColumns,
  CommentData,
  CommentColumns,
}
