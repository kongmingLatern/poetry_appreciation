import {
  columns as UserColumns,
  getData,
} from '@/utils/User'
import {
  columns as PoemColumns,
  getPoemData,
} from '@/utils/Poem'
import {
  columns as CommentColumns,
  getCommentData,
} from '@/utils/Comment'

export {
  getData,
  getPoemData,
  getCommentData,
  UserColumns,
  PoemColumns,
  CommentColumns,
}
